8114要求的规则名称：缓冲区写入操作禁止越界
原来后台配置的检测器：sast_s_21_01（一般数组越界）和sast_s_21_05（结构体数组越界）都不符合要求。
因为缓冲区溢出，需要比较两个数组的大小/类型等是否相同，是否进行拷贝操作。只有这三种情况同时发生才会有缓冲区溢出的问题。
修改后配置的检测器：sast_s_02_50 在此检测器基础上添加部分代码工作量最小，后续，如果，逻辑不够，需要再补充。