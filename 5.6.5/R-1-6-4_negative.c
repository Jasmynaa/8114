//(必要)禁止位运算的结果是逻辑量
//------内容微调8114的“R - 1 - 6 - 4 禁止对逻辑表达式进行位运算”

int main(void)
{
	int x = 0, y = 1, z = 2;
	if ((x == 1) | (y == 2))  //违背
	{
		z = 3;
	}
	if ((x == 3) & (y == 4))  //违背
	{
		z = 5;
	}
	if (x | y)                  //违背
	{
		z = 7;
	}
	return (0);
}
