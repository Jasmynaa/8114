//5.6.7　(必要)禁止移位操作中的移位数大于等于变量长度
//------内容维持但表述修改8114的“R - 1 - 6 - 6 对变量进行移位运算禁止超出变量长度”

int main(void)
{
	unsigned int x, y, z;
	x = 0x00000001;
	y = x << 33;             //违背
	x = 0x80000000;
	z = x >> 33;             //违背
	return (0);
}
