//5.9.2　(必要)for循环控制变量必须使用整数型变量
//------维持特殊的8114的“R - 1 - 9 - 2 for循环控制变量必须使用整数型变量”

int main(void)
{
	float f = 0.0, g = 1.0;
	for (f = 0.0; f < 10.0; f = f + 1.0)    //违背
	{
		g = f + g;
	}
	return (0);
}
